console.log("Hello World!!!!".toUpperCase());

let currentScoreA = 0;

// memory ---- variables

currentScoreA = 1;

let user = "Albert"; // if you use const, you get a type error when you try to reassign a value to it. const is used for values that should not change.
user = "Shiza";

console.log(currentScoreA);
console.log(user);
