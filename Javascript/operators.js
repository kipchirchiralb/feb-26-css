// arithmetic, assignment, comparison, logical, and ternary operators

// arithmetic operators  +, -, *, /, %, **
console.log(4 ** 3);
// % modulus - remainder
console.log(7 % 3); // 1

// assignment operators -- =, +=, -=, *=, /=, %=, **=

let x = 10; // = assinment operator

// comparison operators -- ==, ===, !=, !==, >, <, >=, <=
console.log(50 + 5 == 50 + 5);

// logical operators -- && (AND), || (OR), ! (NOT) -- used to combine or invert boolean values

// ternary operator -- condition ? expressionIfTrue : expressionIfFalse -- a shorthand for if-else statements

// The truth table

// const while  = 990
